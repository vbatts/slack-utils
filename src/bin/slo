#!/usr/bin/env ruby

require 'rubygems'
require 'slackware/utils'


begin
	print_orphaned_files(find_orphaned_config_files())
rescue Interrupt
	exit 0
rescue Exception => e
	puts "ERROR: #{e.message}"
	exit 1
end

